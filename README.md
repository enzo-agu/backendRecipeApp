#Notas:

Opciones de Yargs

````
Options:
      --help     Show help         [boolean]
      --version  Show version number
                                   [boolean]
  -b, --base     base de multiplicar
                         [number] [required]
  -l, --listar   muestra la tabla en consola
       [boolean] [required] [default: false]
  -h, --hasta    Hasta donde va la tabla
                      [number] [default: 10]
                      ```
````
